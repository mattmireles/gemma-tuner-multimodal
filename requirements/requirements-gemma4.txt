# Gemma 4 compatibility stack — install on top of a normal editable install:
#   pip install -e .
#   pip install -r requirements/requirements-gemma4.txt
#
# Base `pyproject.toml` pins transformers<5; Gemma 4 needs transformers>=5.5 for
# model registration and multimodal classes. See README/plans/gemma4-upgrade.md.

transformers>=5.5.0
peft>=0.18.1
