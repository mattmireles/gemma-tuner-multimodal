# Gemma 4 compatibility stack — install on top of a normal editable install:
#   pip install -e .
#   pip install -r requirements/requirements-gemma4.txt
#
# Gemma 4 needs transformers>=5.5 (model registration, multimodal classes).
# Install on top of the base tree from pyproject.toml / requirements.txt.
# See README/plans/gemma4-upgrade.md.

transformers>=5.5.0
peft>=0.18.1
