# -----------------------------------------------------------------
# requirements.txt — derived from pyproject.toml [project.dependencies]
#
# Optional Gemma 4 stack (transformers 5.5+): install after the base tree with
#   pip install -r requirements/requirements-gemma4.txt
#
# pyproject.toml is the single source of truth for dependency bounds.
# To regenerate a pinned lockfile from this, run:
#   pip-compile pyproject.toml --output-file requirements.lock
#
# Torch/torchaudio are intentionally excluded — install per platform:
#   pip install torch torchaudio          (macOS MPS / CPU)
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121  (CUDA)
# -----------------------------------------------------------------

# Hugging Face ecosystem (must match pyproject.toml [project.dependencies])
transformers>=4.46.0
datasets>=4.0.0
accelerate>=0.27.2
evaluate>=0.4.1
peft>=0.9.0

# Audio processing
librosa>=0.10.0,<0.11.0
soundfile>=0.12.0,<0.13.0
scikit-learn>=1.2.0

# Utilities
numpy>=1.26.4
pandas>=1.3.0
tqdm>=4.65.0
packaging>=21.0
filelock>=3.9.0
tabulate>=0.9.0,<0.10.0

# CLI and interaction
typer>=0.9.0
rich>=13.0.0
questionary>=2.0.0,<3.0.0

# Dev
pytest>=7.4,<9.0
ruff>=0.9.3
